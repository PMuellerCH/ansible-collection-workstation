---
name: CI

"on":
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  lint:
    uses: PMuellerCH/shared-actions/.github/workflows/ansible_lint.yml@v1
    with:
      python-version: "3.14"
      install-self-collection: "true"
